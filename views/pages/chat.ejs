<%- include('../partials/header'); %>
<section class="container">

    <main class="main">
        <div class="title">
            <% if (isConnected) { %>
                <h2>Bienvenue sur la page de chat <%= username %> !</h2>
            <% } else { %>
                <h2>Bienvenue sur la page de chat...</h2>
            <% } %>
        </div>

        <div class="content">
            <div class="content__title">
                <h3 class="h3">Chat tout simple</h3>
            </div>
            
            <div class="content__option" id="chat"></div>
            <form class="form" id="form__message">
                <div class="form__content">
                    <div class="input">
                        <input id="message" name="message" placeholder="Écrire un message" type="text">
                    </div>
                
                    <button class="btn-primary">Envoyer</button>
                </div>
            </form>
        </div>
    </main>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/chat.js"></script>

    <%- include('../partials/footer'); %>
